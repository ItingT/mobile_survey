var enroll = document.querySelector('enroll');
 

if (enroll == "Y") {
    return window.location.assign("HTTP://www.google.com")
  } else {
    return window.location.assign("HTTP://www.google.com")
  }

